<!DOCTYPE html>

<html>
    <head>
        
        <title>Predictor</title>
        
        <link rel="stylesheet" href="css/styles.css">
        
        
    </head>

    <body>
        <div class="main-content">
            {{>header}}
            <p>Select a league for predictions.</p>

            <form name="leagueForm" id="leagueForm">
                <input placeholder="Enter previous games to calculate" >
                </br>
                <select name="league" id="league" >
                    {{#each leagues}}
                        <option value="{{id}}">{{country}} - {{name}}</option>
                    {{/each}}
                </select>
                <button name='leagueFixtues'>Get Fixtures By League</button>
                <button name='allFixtues'>Get All Fixtues</button>
            </form>
            <div id="fixture-table-container"></div>
            <p id="message-1"></p>
            <p id="message-2"></p>
        </div>
        
        {{>footer}}
        <script src="js/app.js"></script>
    </body>
</html>